<section id="live_transcript_panel" class="rounded-box border border-base-300 bg-base-100 p-4 text-left shadow-sm" data-testid="live-transcript-panel">
  <%= render "recording_sessions/live_transcript_status", recording_session: recording_session %>
  <%= render "recording_sessions/live_transcript_segments", recording_session: recording_session %>
</section>
